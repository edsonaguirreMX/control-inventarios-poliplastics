# Se genera solo al correr `npx convex dev` por primera vez (queda en .env.local, que no se sube a git).
CONVEX_DEPLOYMENT=

# La usa el frontend (src/server.js y las páginas) para hablar con tu deployment de Convex.
CONVEX_URL=
