{
  "name": "tejaflex-app",
  "version": "0.1.0",
  "private": true,
  "description": "Tejaflex — Control de Materias Primas: aplicación viva (Convex + Express)",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev:backend": "convex dev",
    "dev:web": "node src/server.js",
    "build": "node scripts/build-client.mjs",
    "start": "node src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "convex": "^1.43.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
